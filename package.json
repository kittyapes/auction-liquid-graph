{
  "name": "hypex-auction-liquid-subgraph",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen --debug --output-dir entities/",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service defigods/auction-liquid"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.40.0",
    "@graphprotocol/graph-ts": "^0.29.3"
  }
}
